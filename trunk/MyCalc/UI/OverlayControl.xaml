<UserControl x:Class="MyCalc.UI.OverlayControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.Resources>
            <BooleanToVisibilityConverter x:Key="BoolVisibilityConverter"/>
        </Grid.Resources>
        <Border Background="#7F292929" Visibility="{Binding ShowOverlay, Converter={StaticResource BoolVisibilityConverter}}">
            <TextBlock Text="{Binding OverlayText}" Foreground="White" FontSize="26" FontWeight="DemiBold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>

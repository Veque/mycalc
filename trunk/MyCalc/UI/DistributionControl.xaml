<UserControl x:Class="MyCalc.UI.DistributionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:ui="clr-namespace:MyCalc.UI"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
        <Grid>
            <Border BorderThickness="3,0,0,3" BorderBrush="Black">
                <ItemsControl ItemsSource="{Binding Items}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                </ItemsControl>
            </Border>
            <Border BorderBrush="Black" BorderThickness="2,0,0,0" HorizontalAlignment="Left" Margin="{Binding AverageMargin}">
                <TextBlock Text="{Binding AverageText}" VerticalAlignment="Top" HorizontalAlignment="Left"/>
            </Border>
            <ui:OverlayControl/>
        </Grid>
    </ScrollViewer>
</UserControl>

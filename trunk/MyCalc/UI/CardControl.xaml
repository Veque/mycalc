<UserControl x:Class="MyCalc.UI.CardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Width="32" Height="58">
    <Border CornerRadius="4" BorderThickness="1" BorderBrush="{Binding SuitColor}" Background="White">
        <StackPanel Orientation="Vertical">
            <TextBlock Foreground="{Binding SuitColor}" FontSize="18" FontWeight="Bold" FontFamily="Segoe UI" Text="{Binding ValueText}" HorizontalAlignment="Center" Margin="1,1,1,0" />
            <Image HorizontalAlignment="Center" Source="{Binding SuitImage}" Margin="1,0,1,1" Stretch="None"/>
        </StackPanel>
    </Border>
</UserControl>
